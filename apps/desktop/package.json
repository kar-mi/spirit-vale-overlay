{
  "name": "@svoverlay/desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "bun run src/build.ts",
    "update": "neu update",
    "dev": "bun run prepare && neu build && bun run src/dev.ts",
    "build": "bun run prepare && neu build",
    "package": "bun run prepare && bun run src/select-windows-release-binaries.ts && neu build --clean --release && bun run ../../tooling/release/wrap-portable.ts",
    "test": "bun test src"
  },
  "dependencies": {
    "@kar-mi/spirit-vale-tools-capture": "2.4.1",
    "@neutralinojs/lib": "6.9.0",
    "@svoverlay/contracts": "workspace:*",
    "@svoverlay/desktop-platform": "workspace:*",
    "@svoverlay/overlay": "workspace:*",
    "@svoverlay/ui-kit": "workspace:*",
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@neutralinojs/neu": "11.7.2"
  }
}
