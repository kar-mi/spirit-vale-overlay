import type {
  CombatActorRow,
  CombatEncounterSnapshot,
  CombatSkillRow,
  CombatTimelinePoint,
} from "@kar-mi/spirit-vale-tools-combat";

export type MeterActorRow = CombatActorRow;
export type MeterEncounterSnapshot = CombatEncounterSnapshot;
export type MeterSkillRow = CombatSkillRow;
export type MeterTimelinePoint = CombatTimelinePoint;
