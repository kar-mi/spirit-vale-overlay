{
  "name": "spirit-vale-overlay",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "bun run --filter @svoverlay/launcher build",
    "build:stable": "bun run --filter @svoverlay/launcher build -- --env=stable",
    "check": "bun run typecheck && bun test",
    "dev": "bun run --filter @svoverlay/launcher dev",
    "package:portable": "bun run tooling/release/package-portable.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "verify:portable": "bun run tooling/release/verify-portable.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "resedit": "3.0.2",
    "typescript": "7.0.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "packageManager": "bun@1.3.14"
}
